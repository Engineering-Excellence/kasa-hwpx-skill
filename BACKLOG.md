# BACKLOG — 미반영 잔여 후보

v0.7.1(2026-07-07) 릴리스 + 업스트림 재관찰(2026-07-07) 기준. 어느 머신에서든
이 파일과 README의 버전 이력만 보면 이어서 작업할 수 있다.

## 잔여 후보 (위에서부터 권장 순서, 2026-07-07 kordoc 조사 결과)

기존 3개 업스트림은 재관찰 결과 신규 커밋 없음(아래 표). 신규 후보는 전부
`chrisryugj/kordoc`(TypeScript/Node — 코드 직접 이식 불가, **아이디어·규칙만**
파이썬 표준 라이브러리로 재구현)에서 발굴했다.

1. ~~**신구대조(hwpx_diff.py)**~~ — **완료(2026-07-07, 커밋 대기)**: 문단·표 셀 단위
   비교 보고(추가/삭제/수정/동일 통계 + 셀 diff + 크기 변화), 읽기 전용. 테스트 6케이스.
2. ~~**kasa_lint 보강(편람 대조)**~~ — **완료(2026-07-07, 커밋 대기)**: 시각 두 자리
   (`8:9`→`08:09`), 쌍점 뒤 1타, 물결표 `∼`(U+223C)·`〜` 인식, 요일 괄호 붙임.
   ※ '쌍점 앞 붙임' 검사는 기준 양식 원문("편집용지 : 좌…")과 충돌해 의도적으로 제외.
   테스트 7케이스.
3. ~~**라벨 기반 양식 채우기(label_fill.py)**~~ — **완료(2026-07-07, 커밋 대기)**:
   detect/fill, 라벨 인식(콜론·짧은 명사)+값 거름망, 오른쪽→아래 빈 셀, 채워진 셀
   덮어쓰기 금지, 빈 셀에 `<hp:t>` 없으면 창작 없이 미적중 보고. 테스트 7케이스.
4. ~~**도장 부유 날인(겹침 배치)**~~ — **제외(2026-07-07, 사용자 지시)**: 위험 클래스
   (기준 양식·실물에 '부유+글 앞' pic 패턴 없음 — MI 로고도 글자취급). 재론하려면
   한글이 저장한 부유 도장 실물 샘플 확보가 선행되어야 한다.
5. ~~**금액 한글 병기 lint**~~ — **제외(2026-07-07, 사용자 지시)**: 기안문·계약 관행으로
   KASA 보고서 스코프에 불필요 판단.

## 완료·제외 이력 (2026-07-07 스프린트)

- ~~KASA 표기법 lint~~ — 완료: `kasa_lint.py` + validate 통합 (v0.7.0)
- ~~PreToolUse 가드 훅 3종~~ — 완료: `hooks/` finalize/sample/spec guard (v0.7.0)
- ~~직인/서명·이미지 삽입·편집 (P12/P13)~~ — 완료: `hwpx_image.py`, 실물 열람 검증 (v0.7.0)
- ~~Canine89 unpack 바이트 보존 점검~~ — 완료: 왕복 회귀 테스트 (v0.7.0)
- ~~표 행 높이·정렬~~ — 완료: 데이터 행 높이 동일화 + 숫자만 우측 정렬 (v0.7.1)
- ~~네이티브 각주·하이퍼링크 (P4)~~ — **제외 확정(사용자 결정)**: 검증된 실물 패턴 없는
  XML 창작 = 위험 클래스. 재론하려면 한글 저장 각주 실물 샘플 확보 선행.

## 제외 판단 (재론 시 사용자 결정 필요)

- 수식·차트·도형·테마 지원: KASA 표준양식이 고정이라 수요 없음.
- 문서 유형 확장(기안문·보도자료·계획서): 스킬 스코프 확대라 별도 결정 필요.
  kordoc의 8단계 항목부호(1.→가.→1)→…→㉮) 공문서 모드도 같은 이유로 제외.
- kordoc의 HWP 3.x/5.x 바이너리·PDF 파싱, SVG 레이아웃 렌더: 스코프(HWPX 전용)·
  의존성(파이썬 표준 라이브러리만) 원칙 위반.
- py3.9 호환 작업: 해당 없음(스크립트에 3.10+ 문법 미사용 확인, 2026-07-07).

## 업스트림 관찰 시점 (다음 동기화 때 이 커밋 이후분만 확인)

| 저장소 | 마지막 확인 커밋 | 관찰일 | 비고 |
| --- | --- | --- | --- |
| jkf87/hwpx-skill | `48cf9ab` | 2026-07-07 | 재관찰 — 6/30 이후 신규 커밋 없음 |
| Canine89/hwpxskill | `cb5f25b` | 2026-07-07 | 재관찰 — 6/27 이후 신규 커밋 없음 |
| ai-public-peasant/hwpx-rekian | `e151c96` | 2026-07-07 | 재관찰 — 4/09 이후 신규 커밋 없음 |
| chrisryugj/kordoc | `3c76d6a` (v3.17.0) | 2026-07-07 | 신규 관찰 — 위 후보 1~5 발굴 |

## 작업 시작 절차

```bash
git clone https://github.com/Engineering-Excellence/kasa-hwpx-skill.git
cd kasa-hwpx-skill
python3 -m unittest discover -s tests   # 121개 통과 확인 (Python 3.10+, 외부 의존성 없음)
```

기능 1개 = 커밋 1개(Conventional Commits, 한국어 제목), 회귀 테스트 동반 추가,
`tests/`는 `.skill` 패키지에 미포함 — 기존 관례를 따른다.
**관례·안전 원칙·릴리스 절차 전문은 [AGENTS.md](AGENTS.md) 참고**(에이전트 작업 진입점).
