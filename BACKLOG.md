# BACKLOG — 잔여 작업·제외 판단·업스트림 관찰

v0.10.0(2026-08-05 릴리스) + 업스트림 재관찰(2026-07-14) 기준. 어느 머신에서든
이 파일과 README의 버전 이력만 보면 이어서 작업할 수 있다.

## 지금 남은 일

1. **v0.10.0 한글 실물 열람 확인** — 사용자 지시로 v0.9.0·v0.10.0 모두 실물 확인 **전에**
   태그·릴리스를 진행했다(AGENTS.md 5장의 rc 절차를 두 회차 생략). 실제 참관계획 문서로
   `shift_time.py`를 돌려 한글에서 렌더를 확인하고, 문제가 있으면 v0.10.1로 수정한다.
   확인 순서: `--dry-run`으로 목록 확인 → 실행 → `--report` 대조표 검토 → 한글 열람.
2. ~~시각 표기 정규화 정책 결정~~ — **결정·구현 완료(2026-08-05)**: 옵트인 `--pad-hour`로
   **적용 범위 안에서만** 교정한다(② 채택). lint 완화(③)는 규칙이 실재하므로 부적합,
   현행 유지(①)는 수작업 매핑으로 되돌아가 v0.9.0의 취지에 반해 배제. 문서 전역 정규화
   도구를 따로 두지 않은 이유는 보존 구간(교통편·셔틀)까지 바꾸기 때문이다.
   순연 없이 표기만 고치려면 `--shift "+0m" --pad-hour`.

**신규 기능 후보는 현재 비어 있다.** 다음 후보는 (a) 주간 업스트림 재관찰 보고,
(b) 실물 사용 중 발견되는 사고(예: v0.9.0을 낳은 순연 오치환)에서 발굴한다.

## 완료 이력

| 버전 | 내용 |
| --- | --- |
| v0.7.0 | KASA 표기법 lint(`kasa_lint.py`), PreToolUse 가드 훅 3종, 직인/서명 이미지(`hwpx_image.py`), unpack→repack 바이트 보존 회귀 |
| v0.7.1 | 표 데이터 행 높이 동일화 + 숫자 셀만 우측 정렬 |
| v0.8.0 | 신구대조(`hwpx_diff.py`), 라벨 기반 채우기(`label_fill.py`), 표기법 lint 보강(시각 두 자리·쌍점 띄움·`∼`·요일 괄호) — kordoc 아이디어 반영 1차 |
| v0.8.1 | 범용 GenAI 플랫폼 중립화(AGENTS.md 7장·GEMINI.md, 훅 없는 플랫폼용 수동 게이트) |
| v0.8.2 | 재기안 연쇄 오치환(도미노) 제거 — 원본 기준 동시 치환 |
| v0.9.0 | 시각 일괄 순연(`shift_time.py`) — 범위 지정·제외 우선·드라이런·신구대조표·전건 검산. 표 순번을 `list-tables`와 같은 1-base로 통일 |
| v0.10.0 | 시각 순연 표기 교정 옵션(`--pad-hour`, 적용 범위 안에서만) + `--shift "-1:00"` argparse 오인 수정 |

- 릴리스 페이지: 전 버전(v0.1.0~v0.10.0) GitHub 릴리스를 2026-08-05에 소급 생성했고,
  태그가 없던 v0.4.1도 커밋 `d827a5d`(SKILL.md `version: 0.4.1`)에 소급 태깅했다.
  v0.5.0은 롤백 버전이라 prerelease + 경고로 표시한다.

## 제외 판단 (재론 시 사용자 결정 필요)

- **네이티브 각주·하이퍼링크(P4)** — 검증된 실물 패턴 없는 XML 창작은 위험 클래스.
  재론하려면 한글이 저장한 각주 실물 샘플 확보가 선행되어야 한다.
- **도장 부유 날인(겹침 배치)** — 같은 이유(기준 양식·실물에 '부유+글 앞' pic 패턴 없음.
  MI 로고도 글자취급). 부유 도장 실물 샘플이 있어야 재론 가능.
- **금액 한글 병기 lint** — 기안문·계약 관행으로 KASA 보고서 스코프에 불필요(2026-07-07).
- 수식·차트·도형·테마: KASA 표준양식이 고정이라 수요 없음.
- 문서 유형 확장(기안문·보도자료·계획서): 스킬 스코프 확대라 별도 결정 필요.
  kordoc의 8단계 항목부호(1.→가.→1)→…→㉮) 공문서 모드도 같은 이유로 제외.
- kordoc의 HWP 3.x/5.x 바이너리·PDF 파싱, SVG 레이아웃 렌더: 스코프(HWPX 전용)·
  의존성(파이썬 표준 라이브러리만) 원칙 위반.
- jkf87 v1.7.0의 rhwp(Node/WASM vendor) 기반 HWP→HWPX 변환: 같은 원칙 위반(2026-07-14).
- kordoc v3.18.0~v4.0.6: 서식 프로필(#41)·공문서 생산로직(v4.0.0)·인라인 강조/리스트
  depth(v4.0.5)는 md 재생성·문서유형 확장 관심사라 해당 없음(우리는 서식 보존 편집).
  표기법 URL 오탐 가드(v4.0.1)는 우리 쌍점 규칙이 한글 선행을 요구해 이미 방어됨,
  캡션 안 중첩표 텍스트 유실(#46)은 우리 추출이 평면 `<hp:t>` 스캔이라 결함 클래스
  자체가 없음을 확인(2026-07-14).
- py3.9 호환 작업: 해당 없음(스크립트에 3.10+ 문법 미사용 확인, 2026-07-07).

## 업스트림 관찰 시점 (다음 동기화 때 이 커밋 이후분만 확인)

> **자동 관찰 중(2026-07-08~):** 클라우드 루틴 "kasa-hwpx 업스트림 주간 재관찰"이
> 매주 화요일 09:07(KST) 아래 4개 저장소를 재관찰해 보고한다(관찰·보고 전용, 커밋 없음).
> 수동 재관찰은 불필요하며, 루틴이 변동을 보고하면 사용자 세션에서 이 표와 후보를 갱신한다.
> 관리: https://claude.ai/code/routines/trig_01PWR4tshH2JCaKKyXNRuomM

| 저장소 | 마지막 확인 커밋 | 관찰일 | 비고 |
| --- | --- | --- | --- |
| jkf87/hwpx-skill | `e5e35cb` (v1.7.0) | 2026-07-14 | rhwp HWP→HWPX 변환 3커밋 — 제외(위) |
| Canine89/hwpxskill | `cb5f25b` | 2026-07-14 | 재관찰 — 신규 커밋 없음 |
| ai-public-peasant/hwpx-rekian | `e151c96` | 2026-07-14 | 재관찰 — 신규 커밋 없음 |
| chrisryugj/kordoc | `96dd0dd` (v4.0.6) | 2026-07-14 | v3.18.0~v4.0.6 조사 — 반영 대상 없음(위) |

※ 2026-07-14 회차는 클라우드 루틴 실행이 실패해 사용자 세션에서 수동 수행함.
※ 2026-07-21 이후 회차 결과는 미반영 — 이어받는 세션은 루틴 보고를 먼저 확인할 것.

## 작업 시작 절차

```bash
git clone https://github.com/Engineering-Excellence/kasa-hwpx-skill.git
cd kasa-hwpx-skill
python3 -m unittest discover -s tests   # 153개 통과 확인 (Python 3.10+, 외부 의존성 없음)
```

기능 1개 = 커밋 1개(Conventional Commits, 한국어 제목), 회귀 테스트 동반 추가,
`tests/`는 `.skill` 패키지에 미포함 — 기존 관례를 따른다.
**관례·안전 원칙·릴리스 절차 전문은 [AGENTS.md](AGENTS.md) 참고**(에이전트 작업 진입점).
